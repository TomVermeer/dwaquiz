{
  "name": "shared",
  "module": "dist",
  "version": "1.0.0",
  "description": "Shared logic for all Quizzer clients",
  "author": "Erik Knaake (598368)",
  "license": "ISC",
  "scripts": {
    "babelBuild": "babel src --out-dir dist",
    "build:linux": "npm run babelBuild && cp package.json dist",
    "build": "npm run babelBuild && copy package.json dist"
  },
  "dependencies": {
    "immer": "^7.0.9",
    "react": "^16.13.1",
    "bootstrap": "^4.5.3",
    "node-sass": "^4.14.1",
    "react-bootstrap": "^1.3.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "@babel/preset-react": "^7.10.4"
  }
}
